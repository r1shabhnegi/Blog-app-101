import { Loader } from "lucide-react";

const Spinner = () => {
  return (
    <Loader className='flex-1 m-0 mx-auto text-center h-min animate-spin size-10' />
  );
};
export default Spinner;
