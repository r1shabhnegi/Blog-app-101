import { Loader } from "lucide-react";

const Spinner = () => {
  return (
    <Loader className='flex-1 h-auto m-0 mx-auto text-center animate-spin size-10' />
  );
};
export default Spinner;
